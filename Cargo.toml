[package]
name = "aski-core"
version = "0.1.0"
edition = "2021"
description = "aski Kernel schema — World struct, queries, and derivation generated from kernel.aski"

[dependencies]
rkyv = { version = "0.8", default-features = false, features = [
    "little_endian", "pointer_width_32", "alloc", "bytecheck",
] }

[build-dependencies]
